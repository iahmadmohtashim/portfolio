import React from 'react';import profileImage from '../assets/images/Mohtashim.jpg'; // Replace with your image pathexport default function Header(){/*const Header = () => {*/  const handleScroll = (section) => {    document.getElementById(section)?.scrollIntoView({ behavior: 'smooth' });  };/*const LazyImage = ({ src, alt }) => {  const [loaded, setLoaded] = useState(false);*/  return (    <header className="header">      <nav className="nav-container">        <div className="logo">          {/* Replace with your logo or initials */}          <span>Profile</span>        </div>        <ul className="nav-links">          <li><button onClick={() => handleScroll('home')}>Home</button></li>          <li><button onClick={() => handleScroll('about')}>About</button></li>          <li><button onClick={() => handleScroll('skills')}>skills</button></li>          <li><button onClick={() => handleScroll('projects')}>Projects</button></li>          <li><button onClick={() => handleScroll('contact')}>Contact</button></li>        </ul>      </nav>      <div className="hero-section">        <div className="hero-content">          <h4 className="greeting">Hi, I'm</h4>          <h1 className="name">Mohtashim Ahmad</h1>          <h2 className="title">Senior Software Engineer</h2>          <p className="description">            Building digital experiences that matter specializing in modern web technologies            and java based frameworks for modern web application and user-centric design.          </p>          <button            className="cta-button"            onClick={() => handleScroll('contact')}          >            Let's Connect          </button>        </div>        <div className="profile-image">          {/* Replace with your actual image */}          {<img src ={profileImage} alt="Profile" loading = "lazy" className="profile-img" />}         {/*<img            src="{profileImage}"            alt="Profile"            className="profile-img"          />*/}          {/*<img                src={loaded ? src : 'Mohtashim.jpg'}                alt={alt}                loading="lazy"                onLoad={() => setLoaded(true)}                className={loaded ? 'loaded' : 'loading'}              />*/}        </div>      </div>    </header>  );};